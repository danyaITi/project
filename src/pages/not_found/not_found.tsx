import { NotFoundText } from 'shared/ui/notFound_text';
import styles from './styles.module.scss';

export const NotFound = () => (
    <section className={styles.notFoundPage}><NotFoundText /></section>
);
